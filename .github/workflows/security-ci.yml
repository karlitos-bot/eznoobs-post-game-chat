name: EZNOOBS Security CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  security-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Security regression checks
        run: npm run security:check

      - name: Mobile regression checks
        run: npm run mobile:check

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
